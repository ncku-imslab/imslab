<!DOCTYPE html>
<html>
  <head>
    {{> head}}
    <link rel="stylesheet" href="{{baseUrl}}css/layout.css" />
	</head>
	<body>
    {{> header}}
    <div class="container-fluid">
      <ul class="nav nav-tabs">
      {{#each honors}}
        <li {{#if @first}} class="active" {{/if}}><a href="#hon-{{@index}}">{{@key}}</a></li>
      {{/each}}
        <li class="toTop"><a href="#"></a></li>
      </ul>
      {{#each honors}}
      <div class="block" id="hon-{{@index}}">
        <hr><h3 class="title">{{@key}}</h3><hr>
        <ul class="enableOpen">
          {{#each this}}
          <li id="list-{{@../index}}-{{@key}}">{{@key}}</li>
            <div id="content-{{@../index}}-{{@key}}" class="list-content {{#if @first}} active {{/if}}">
              {{#each this}}
              <div class="row" style="margin:5px -15px">
                <div class="col-md-3">{{this.name}}</div>
                <div class="col-md-9">{{this.content}}</div>
              </div>
              {{/each}}
            </div> <!-- /.list-content -->
          {{/each}}
        </ul>
      </div> <!-- /.block -->
      {{/each}}
    </div> <!-- /.container-fluid -->
    {{> footer }}
  </body>
</html>


