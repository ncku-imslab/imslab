module.exports= function(hbs){
  hbs.registerHelper('ifThreeOne', function(conditional, options) {
    if((conditional % 3) == 1) {
      return options.fn(this);
    } else {
      return options.inverse(this);
    }
  });
}
