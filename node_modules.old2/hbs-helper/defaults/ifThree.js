module.exports= function(hbs){
  hbs.registerHelper('ifThree', function(conditional, options) {
    if((conditional % 3) == 0) {
      return options.fn(this);
    } else {
      return options.inverse(this);
    }
  });
}
