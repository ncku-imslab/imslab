module.exports= function(hbs){
  hbs.registerHelper('ifThreeTwo', function(conditional, options) {
    if((conditional % 3) == 2) {
      return options.fn(this);
    } else {
      return options.inverse(this);
    }
  });
}
