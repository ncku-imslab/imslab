var inline = require('../');
var fs = require('fs');
var path = require('path');

var opt = {
	"minifyCSS": true,
	"minifyJS": true,
	"removeComments":true,
	"collapseWhitespace": true
}

var inline_test = inline(path.join(__dirname, 'inline.html'), opt, function(html) {
	console.log(html);
	fs.writeFileSync(path.join(__dirname, 'test_after2.html'), html)
});

