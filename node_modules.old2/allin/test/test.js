var inline = require('../');
var fs = require('fs');
var path = require('path');

var inline_test = inline(path.join(__dirname, 'inline.html'), function(html) {
	console.log(html);
	fs.writeFileSync(path.join(__dirname, 'test_after.html'), html)
});

